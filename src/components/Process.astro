---
const steps = [
	{
		title: "Discover",
		description: "We gather requirements, define scope, and confirm the technical plan.",
	},
	{
		title: "Design",
		description: "We design user flows and interfaces tailored to your needs.",
	},
	{
		title: "Build",
		description: "We build, test, and harden the solution with modern stacks.",
	},
	{
		title: "Launch + Grow",
		description: "We deploy, monitor, and improve the solution over time.",
	},
];
---

<section id="process" class="border-y border-subtle bg-section px-6 py-16 sm:py-20 reveal" data-animate>
	<div class="mx-auto w-full max-w-6xl">
		<div class="flex flex-col gap-4">
			<p class="text-sm font-semibold uppercase tracking-[0.3em] text-accent">Process</p>
			<h2 class="text-3xl font-semibold text-primary md:text-4xl">A clear, in-house delivery.</h2>
			<p class="max-w-2xl text-base text-muted">
				We manage the full delivery lifecycle with clear milestones and direct communication.
			</p>
		</div>
		<div class="mt-8 space-y-4 sm:mt-10">
			{steps.map((step, index) => (
				<div class="flex gap-4">
					<span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-subtle text-xs font-semibold text-primary">
						0{index + 1}
					</span>
					<div>
						<h3 class="text-lg font-semibold text-primary">{step.title}</h3>
						<p class="mt-2 text-sm text-muted">{step.description}</p>
					</div>
				</div>
			))}
		</div>
	</div>
</section>
